#include <bits/stdc++.h>
using namespace std;

int func2(int arr[], int N) {
    for (int i=0; i < N; i++) {
        int tmp = 100 - arr[i];
        
    }
}

int main()
{
    ios::sync_with_stdio(0); cin.tie(0);

    int arr[] = {1, 52, 48};
    int N = 3;

    func2(arr[], N);

    return 0;
}